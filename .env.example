# Ollama
OLLAMA_API_BASE=http://localhost:11434
# Optional: Hugging Face token for the cloud fallback model (higher rate limits)
HF_TOKEN=
# Optional: where the real crawler drops its output (used by scripts/ingest_crawler.py)
CRAWLER_INPUT_PATH=
